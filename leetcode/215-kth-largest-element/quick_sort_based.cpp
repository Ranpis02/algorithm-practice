#include <iostream>
#include <vector>

using namespace std;

class QuickSort
{
public:
  void sort(vector<int> nums)
  {
  }

private:
  void quickSelect(int l, int r, vector<int> arr)
  {
    if (l == r)
      return;

    int i = l + 1;
    for (int i = l + 1; i <= r; i++)
    {
      // if(arr[l])
    }
  }
};

int main()
{

  return 0;
}
